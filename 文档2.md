# 交接文档

## fast_lio_sam

### 其中包含两部分功能：

> (1)3d建图，结果为关键帧点云、轨迹和描述子，可以保存为点云地图形式


### 如何使用：

```
ros2 launch fast_lio_sam run_mid360.py               // (1)mid360建图
ros2 launch fast_lio_sam run_airy.py                 // (2)airy建图
```

建图launch中的选择：默认选择ros2_mid360.yaml



### 建图重要配置参数(ros2_mid360.yaml)：

```bash
lidar_topic：lidar话题
imu_topic：imu话题
gnss_topic：gps话题，这个比较特殊对于不同的厂商的组合导航，需要适配他的驱动，一般需要接受到双天线组合导航的位置和方向

lidar_type：（重要）雷达类型，1是livox系列雷达，2是机械雷达
timestamp_unit：（重要）每个点云时间戳的单位，一般为2-microsecond

loop_closure_search_radius：（重要）回环检测搜索距离
extrinsic_T：（重要）lidar->imu的外参，平移部分
extrinsic_R：（重要）lidar->imu的外参，旋转部分
extrinsic_gnss_T：（重要）lidar->gnss的外参，平移部分
extrinsic_gnss_R：（重要）lidar->gnss的外参，旋转部分

ground_constraint_enable: 是否使用地面约束，用于减少z轴漂移
ground_constraint_angle: 添加地面约束的角度条件阈值，180度为永远添加
save_globalmap_en: 是否保存整个点云地图
save_resolution: 保存点云地图的分辨率
save_keyframe_en: 是否保存关键帧点云，保存后可以做例如动态过滤的后处理
save_pgm: 是否保存二维栅格地图
map_path: 所有结果保存的路径
```


## fastlio_localization

定位程序，launch中，默认选择ros2_mid360_indoor.yaml



### 如何使用：

```
ros2 launch fastlio_localization run_mid360_indoor.py
```

### 定位重要配置参数(ros2_mid360_indoor.yaml)：

```bash
run_mid360_indoor.py
    static_tf: （重要）用于发布lidar->base_link外参的tf树
        arguments=[
            '--x', '0.0',
            '--y', '0.0',
            '--z', '0.0',
            '--roll', '0.0',
            '--pitch', '0.0',
            '--yaw', '0.0',
            '--frame-id', 'base_link',
            '--child-frame-id', 'lidar'
        ]

map_path: 地图读取路径

extrinsic_T：（重要）lidar->imu的外参，平移部分
extrinsic_R：（重要）lidar->imu的外参，旋转部分

lla: (重要, 使用gps时可选)UTM/ENU坐标系的锚点(经纬高)

utm_origin: (使用gps时可选)
    zone: utm坐标系参数配置
    east: utm坐标系参数配置
    north: utm坐标系参数配置
    up: utm坐标系参数配置

bbs3d:
    bbs3d_min_level_res: 有室内场景: 0.5; 无室内场景: 1.0

bnb3d:
    bnb3d_linear_xy_window_size: 分支定界xy方向搜索范围
    bnb3d_linear_z_window_size: 分支定界z方向搜索范围
    bnb3d_angular_search_window: 分支定界yaw搜索范围

gicp:
    gicp_fitness_score: gicp校验阈值
```
